//----------------------------------------------------------------------------------------------------//

Legacy backlog из старого TODO.txt

- app/src/main/java/com/example/chessboard/ui/screen/trainSingleGame/TrainSingleGameScreen.kt:62 ignores the result of app/src/main/java/com/example/chessboard/repository/DataBaseProvider.kt:120. Even if decreasing the line weight fails because the training was already deleted or the game is no longer present, the code still calls onTrainingFinished(result) and exits as if persistence succeeded. That can desync UI flow from actual training state in DB.

- app/src/main/java/com/example/chessboard/ui/screen/CreateTrainingScreen.kt:98 silently fails when saving an empty training. app/src/main/java/com/example/chessboard/service/TrainingService.kt:20 returns null for an empty list, and the UI just return@launch at line 114. Since the Save button is always enabled at app/src/main/java/com/example/chessboard/ui/screen/CreateTrainingScreen.kt:148, a user with zero games gets no feedback and no state change.

- после 1 тренировки с разными весами игра удаляется (нужно сохранять)
- партии надо грузить не все, а пачками штук по 10-20 чтоб не тормозило
- на некоторых экранах при переходах доску надо сделать больше
- местами при переходах экранов как будто дёргается
- режим hard: за ошибки снимается прогресс частично
- написать user flow
- пройти по всем flow
- исправить дизайн на всех экранах
- настроить миграции
- не забыть что после 10к строк статистика удаляется
- визуал создания тренировки по статистике
- кнопку некст после прохождения игры
- если поставить макс ходов 1 то можно быстро набить статистику
- перенести экран настроек

//----------------------------------------------------------------------------------------------------//

Текущее состояние рефакторинга training/template editor
Подродное описание, чтобы продолжить на другом копьютере.

Дата сводки: 2026-04-14
Проект: /home/lokis/AndroidProjects/ChessBoard

Цель работы

- Подготовить общий editor-layer для экрана редактирования тренировки и будущего экрана редактирования шаблона тренировки.
- Не делать второй почти-клон экрана, а выносить общие модели, preview/session слой и UI-компоненты.

Что уже сделано

1. Вынесены общие модели editor-layer

Файл:
- app/src/main/java/com/example/chessboard/ui/screen/training/TrainingEditorModels.kt

Что лежит в этом файле:
- TrainingGameEditorItem
- decreaseTrainingGameWeight(games, gameId)
- increaseTrainingGameWeight(games, gameId)
- removeTrainingGame(games, gameId)
- GameEntity.toTrainingGameEditorItem(weight)

Что изменено:
- CreateTrainingScreen.kt больше не хранит shared модель TrainingGameEditorItem внутри себя.
- CreateTrainingScreen.kt и EditTrainingScreen.kt используют общие list-based helper functions.

Почему это важно:
- shared editor-model больше не привязан к create-flow.
- это база для reuse в будущем EditTrainingTemplateScreen.

2. Добавлены unit tests для shared helper functions

Файл:
- app/src/test/java/com/example/chessboard/ui/screen/training/TrainingEditorModelsTest.kt

Что покрыто:
- decreaseTrainingGameWeight уменьшает вес только нужной партии
- decreaseTrainingGameWeight не опускает вес ниже 1
- increaseTrainingGameWeight увеличивает вес только нужной партии
- removeTrainingGame удаляет только нужную партию
- все 3 helper functions делают no-op для неизвестного gameId

Проверка:
- :app:testDebugUnitTest --tests com.example.chessboard.ui.screen.training.TrainingEditorModelsTest

3. Вынесен shared preview/session layer

Файл:
- app/src/main/java/com/example/chessboard/ui/screen/training/TrainingEditorPreview.kt

Что лежит в файле:
- ParsedTrainingEditorGame
- TrainingEditorBoardSession
- rememberTrainingEditorBoardSession(games)
- resolveTrainingPreviewBoardOrientation(game)

Что важно:
- ParsedTrainingEditorGame сейчас хранит только derived data:
  - uciMoves
  - moveLabels
- поле game из ParsedTrainingEditorGame было удалено как лишнее
- orientation теперь берётся из исходного TrainingGameEditorItem через gamesById

Что это дало:
- preview logic больше не зашита прямо в EditTrainingScreen.kt
- этот слой можно будет переиспользовать для template editor

4. Вынесен shared UI одной партии

Файл:
- app/src/main/java/com/example/chessboard/ui/screen/training/TrainingEditorGameSection.kt

Что лежит в файле:
- TrainingEditorGameSectionState
- TrainingEditorGameSectionActions
- TrainingEditorPrimaryAction
- TrainingEditorGameSection(...)
- внутренние private helper composables:
  - TrainingEditorGameHeader(...)
  - RenderTrainingGameEcoBadge(...)
  - TrainingEditorGameCard(...)
  - RenderPrimaryGameActionButton(...)
  - resolveTrainingEditorCurrentMoveLabel(...)

Что делает TrainingEditorGameSection:
- показывает title / eco / weight controls
- показывает preview board для selected game
- показывает card с:
  - Edit
  - reset / prev / next
  - current move label
  - optional primary action button
  - Move Sequence

Что важно в текущем API:
- сигнатура уже сгруппирована:
  - state
  - actions
  - primaryAction
  - modifier
- canUndo/canRedo больше не передаются снаружи, а вычисляются от:
  - state.isSelected
  - state.gameController.canUndo/canRedo

5. Упрощён API MoveLegendSection

Файл:
- app/src/main/java/com/example/chessboard/ui/screen/training/TrainingComponents.kt

Что изменено:
- в MoveLegendSection(...) теперь есть default values для:
  - canUndo = false
  - canRedo = false
  - onPrevMoveClick = {}
  - onNextMoveClick = {}
  - onResetMovesClick = {}

Почему это важно:
- вызовы MoveLegendSection в режиме showNavControls = false стали короче
- исчезли лишние заглушки в вызове из TrainingEditorGameSection

6. EditTrainingScreen переведён на shared слои

Файл:
- app/src/main/java/com/example/chessboard/ui/screen/training/EditTrainingScreen.kt

Что уже использует экран:
- TrainingEditorModels.kt
- TrainingEditorPreview.kt
- TrainingEditorGameSection.kt

Что осталось внутри EditTrainingScreen.kt:
- loading state для edit training
- save success state
- unsaved changes flow
- save/update logic
- training-specific UI:
  - Random button
  - move depth
  - open game editor navigation
  - start training action

То есть:
- EditTrainingScreen уже частично “облегчён”
- но ещё не преобразован в общий TrainingCollectionEditorScreen

7. Добавлены component tests для TrainingEditorGameSection

Файл:
- app/src/androidTest/java/com/example/chessboard/ui/screen/training/TrainingEditorGameSectionTest.kt

Что покрыто стабильными тестами:
- рендер title / eco / weight / Move Sequence
- нажатия на Increase / Decrease вызывают callbacks
- primary action виден и вызывает callback
- primary action скрыт при primaryAction = null
- Move Sequence скрыт, когда parsedGame = null
- клик по невыбранной карточке вызывает onSelect

Дополнительный test tag:
- app/src/main/java/com/example/chessboard/ui/UiTestTags.kt
  - TrainingEditorGameCardTestTag

Куда повешен tag:
- TrainingEditorGameSection.kt на CardSurface в TrainingEditorGameCard(...)

Что пробовали, но не оставили:
- был тест на клик по конкретному move chip в LazyRow
- он оказался flaky на instrumentation runner из-за lazy item materialization / semantics
- тест удалён, чтобы suite был стабильным

Проверка:
- :app:compileDebugAndroidTestKotlin
- :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.example.chessboard.ui.screen.training.TrainingEditorGameSectionTest
- итог: 5/5 passed

Что ещё уже было сделано раньше в этой сессии, но не относится напрямую к текущему рефакторингу

1. Исправлена ориентация preview board при выборе партии в EditTrainingScreen

Файлы:
- app/src/main/java/com/example/chessboard/ui/screen/training/CreateTrainingScreen.kt
- app/src/main/java/com/example/chessboard/ui/screen/training/EditTrainingScreen.kt
- app/src/androidTest/java/com/example/chessboard/ui/screen/training/EditTrainingScreenTest.kt

Суть:
- TrainingGameEditorItem теперь несёт sideMask
- preview board в EditTrainingScreen выставляет orientation по sideMask выбранной партии

2. Ранее были добавлены test tags и fixed UI tests для маленького/нестандартного экрана

Файлы:
- app/src/main/java/com/example/chessboard/ui/UiTestTags.kt
- PositionEditorScreen.kt
- GameEditorScreen.kt
- EditTrainingScreen.kt
- соответствующие androidTest

Суть:
- добавлены list/container tags
- тесты теперь скроллят родительские контейнеры, а не пытаются работать с offscreen children напрямую

Что осталось сделать по плану

Следующий большой шаг:
- собрать общий экран уровня training/template collection editor

Рекомендуемый следующий коммит:
- Extract shared training collection editor screen

Что в него должно войти:

1. Создать новый shared screen

Новый файл, рекомендуемое имя:
- app/src/main/java/com/example/chessboard/ui/screen/training/TrainingCollectionEditorScreen.kt

Идея:
- это будет общий экран для редактирования набора партий
- EditTrainingScreen и будущий EditTrainingTemplateScreen будут thin wrappers around it

Что примерно должно жить внутри:
- общий scaffold
- back handling
- unsaved changes dialog
- общий list state
- wiring между TrainingEditorBoardSession и TrainingEditorGameSection
- top bar save action
- reusable slots / hooks для training-specific и template-specific отличий

Что желательно передавать параметрами:
- screenTitle
- initial name
- initial games
- games count label
- onBackClick
- onNavigate
- onSave
- onOpenGameEditorClick
- optional topBarActions
- optional headerContent
- optional primaryAction per game

2. После этого перевести текущий EditTrainingScreen на shared screen

Тогда в EditTrainingScreen.kt должны остаться:
- training loading state
- training save/update logic
- training success dialog
- training-specific actions:
  - Random
  - move depth
  - Start training for selected game

3. Только после этого делать реальный EditTrainingTemplateScreen

Почему не раньше:
- иначе будет дублирование текущего edit training UI
- shared base уже почти готова, не хватает общего screen-layer

Что нужно для будущего template editor

1. Data/service layer

Файл:
- app/src/main/java/com/example/chessboard/service/TrainingTemplateService.kt

Сейчас есть:
- getAllTemplates()
- getTemplateById(templateId)
- deleteTemplate(templateId)
- createTemplate(name)
- addGame(templateId, gameId, weight)
- removeGame(templateId, gameId)
- getGames(templateId)

Чего не хватает:
- bulk update API уровня training service

Рекомендуемый новый метод:
- updateTemplateFromGames(templateId, games, name): Boolean

Пример по смыслу:
- брать template по id
- если template не найден, return false
- если games пустой, решить отдельно:
  - либо return false
  - либо разрешить пустой template
- обновлять:
  - name
  - gamesJson = OneGameTrainingData.toJson(games)

2. Реальный template editor screen

Сейчас MainActivity всё ещё ведёт на placeholder:
- app/src/main/java/com/example/chessboard/MainActivity.kt
  - ScreenType.EditTrainingTemplate -> EditTrainingTemplatePlaceholderScreenContainer(...)

Placeholder файл:
- app/src/main/java/com/example/chessboard/ui/screen/training/EditTrainingTemplatePlaceholderScreen.kt

Что нужно сделать позже:
- заменить placeholder на real container
- рекомендуемое новое имя:
  - EditTrainingTemplateScreen.kt

Внутри контейнера понадобятся:
- EditTrainingTemplateLoadState
- EditTrainingTemplateSaveSuccess
- loadEditTrainingTemplateState(...)
- saveEditedTrainingTemplate(...)
- вызов общего TrainingCollectionEditorScreen(...)

Поведение template editor на первом этапе:
- редактирование имени
- изменение весов
- удаление игры
- preview board
- move legend
- save
- unsaved changes dialog
- без Random
- без move depth
- primary action на карточке можно пока не показывать

Что сейчас НЕ сделано

- нет общего TrainingCollectionEditorScreen
- EditTrainingScreen ещё не стал thin wrapper
- Template editor по-прежнему placeholder
- TrainingTemplateService не умеет bulk update
- MainActivity по-прежнему маршрутизирует EditTrainingTemplate на placeholder

Рекомендуемый порядок дальнейшей работы

1. Сделать TrainingCollectionEditorScreen
2. Перевести EditTrainingScreen на него
3. Добавить updateTemplateFromGames в TrainingTemplateService
4. Реализовать EditTrainingTemplateScreenContainer
5. Подключить MainActivity к реальному template editor
6. Добавить тесты уже на template flow и общий shared editor flow

Что проверять после продолжения работы

- compileDebugKotlin
- compileDebugAndroidTestKotlin
- targeted android tests для TrainingEditorGameSectionTest
- existing EditTrainingScreen tests
- при появлении template editor:
  - targeted android tests на template screen

Коммиты, которые по сути уже закрыты в этой ветке/сессии

1. Extract training editor helpers and add unit tests
2. Extract shared training editor preview session
3. Extract shared training editor game section and add component tests
